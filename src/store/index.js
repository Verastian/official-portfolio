import Vue from 'vue'
import Vuex from 'vuex'
import getDataRepos from '../store/modules/getDataRepos'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    getDataRepos,
  }
})
